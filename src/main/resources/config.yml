debug-mode: false
data-type: JSON # You can choose JSON or SQLite
macro-limits: # In order for this to work, you need Vault
  enabled: true
  groups: # Give the chatmacro.macro.unlimited permission for unlimited macros
    default: 1
    vip: 2
    mvp: 3
